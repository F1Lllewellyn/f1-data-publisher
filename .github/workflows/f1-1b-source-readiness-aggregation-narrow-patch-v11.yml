name: F1 1B Source Readiness Aggregation Patch Workflow Stub v15

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: f1-1b-source-readiness-aggregation-patch-stub-v15-${{ github.ref }}
  cancel-in-progress: false

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: Explain superseded patch workflow
        shell: bash
        run: |
          set -euo pipefail
          echo "This temporary patch workflow has been superseded by the v15 narrow local installer."
          echo "It intentionally performs no repository changes."
          echo "Run F1 Peak Elite Control Room - One Click v1 for validation."
