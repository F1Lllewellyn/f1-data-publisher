F1 Peak-Elite System Installer v3 - Single Click

Double-click ONLY:
  1_DOUBLE_CLICK_ME_INSTALL_F1_PEAK_ELITE_SYSTEM_v3.cmd

This version is self-contained. It embeds the installer payload inside the .cmd file, so it should not fail with the missing .ps1 error caused by Windows compressed-folder temp extraction.

Target repository assumed by default:
  C:\Users\Matt Llewellyn\OneDrive\Documents\GitHub\f1-data-publisher

After it runs, GitHub Actions should open. Run:
  F1 Peak Elite Control Room - One Click v1
with:
  operation=full_safe_chain
  commit_outputs=true
  run_forecast_gate=false
