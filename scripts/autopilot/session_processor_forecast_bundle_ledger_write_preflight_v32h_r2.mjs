const a=process.argv.join(' ');const bad=['--execute true','--activate true','--allow-live true','--allow-ledger-write true'].filter(x=>a.includes(x));const ok=!bad.length;console.log(JSON.stringify({ok,forecast_bundle_ledger_write_preflight_status:ok?'ready':'blocked',production_automation:'OFF',forecast_gate:'OFF',promotion_allowed:false,forecast_bundle_ledger_written:false,canonical_workbook_overwrite:false,notification_sent:false,blockers:bad},null,2));process.exit(ok?0:1);
