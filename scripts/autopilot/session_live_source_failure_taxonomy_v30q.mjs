import fs from 'node:fs'; import path from 'node:path'; const DEFAULT_POLICY = 'scripts/autopilot/session_live_source_hardening_policy_v30q.json'; const DEFAULT_OUT = 'health/session_live_source_failure_taxonomy_v30q_status.json'; const DEFAULT_LOG = ''; function nowIso() { return new Date().toISOString(); } function parseArgs(argv) { const args = { policy: DEFAULT_POLICY, out: DEFAULT_OUT, log: DEFAULT_LOG, adapter: '', validation: '', probe: '', cache: '', mode: 'dry_run', allowLive: false }; for (let i = 2; i < argv.length; i += 1) { const key = argv[i]; const value = argv[i + 1]; if (key === '--policy' && value) { args.policy = value; i += 1; } else if (key === '--out' && value) { args.out = value; i += 1; } else if (key === '--log' && value) { args.log = value; i += 1; } else if (key === '--adapter' && value) { args.adapter = value; i += 1; } else if (key === '--validation' && value) { args.validation = value; i += 1; } else if (key === '--probe' && value) { args.probe = value; i += 1; } else if (key === '--cache' && value) { args.cache = value; i += 1; } else if (key === '--mode' && value) { args.mode = value; i += 1; } else if (key === '--allow-live' && value) { args.allowLive = value === 'true'; i += 1; } } return args; } function readJsonMaybe(filePath) { if (!filePath || !fs.existsSync(filePath)) return null; return JSON.parse(fs.readFileSync(filePath, 'utf8')); } function writeJson(filePath, payload) { fs.mkdirSync(path.dirname(filePath), { recursive: true }); fs.writeFileSync(filePath, JSON.stringify(payload, null, 2) + '\n', 'utf8'); } function collectSources(...payloads) { const byId = new Map(); function add(sourceId, entry) { if (!sourceId) return; const existing = byId.get(sourceId) || { source_id: sourceId, evidence: [] }; existing.evidence.push(entry); byId.set(sourceId, existing); } for (const payload of payloads) { if (!payload) continue; if (Array.isArray(payload.sources)) { for (const source of payload.sources) { add(source.source_id || source.id || source.name, { kind: 'source', source }); } } if (Array.isArray(payload.requests)) { for (const request of payload.requests) { add(request.source_id || request.source || request.adapter, { kind: 'request', request }); } } if (Array.isArray(payload.results)) { for (const result of payload.results) { add(result.source_id || result.source || result.adapter, { kind: 'result', result }); } } if (payload.source_id) add(payload.source_id, { kind: 'payload', payload }); if (payload.adapter) add(payload.adapter, { kind: 'payload', payload }); } return Array.from(byId.values()); } function normalizeStatus(value) { return String(value || '').toLowerCase(); } function classifyEvidence(source, policy) { const failures = []; const warnings = []; let rowEvidence = 0; let fallbackAvailable = false; let liveBlocked = false; for (const item of source.evidence) { const obj = item.result || item.request || item.source || item.payload || {}; const status = normalizeStatus( obj.status || obj.fetch_status || obj.validation_status || obj.readiness_status || obj.cache_status || obj.decision ); const error = normalizeStatus( obj.error || obj.error_code || obj.failure || obj.failure_type || obj.reason ); const rows = Number(obj.row_count ?? obj.rows ?? obj.record_count ?? obj.result_count ?? 0); if (Number.isFinite(rows) && rows > 0) rowEvidence += rows; if (obj.cache_available === true || obj.fallback_available === true || status.includes('cache_artifact_ready')) { fallbackAvailable = true; } if (status.includes('blocked_live') || status.includes('live_disabled') || obj.live_fetch_enabled === false) { liveBlocked = true; } if (status.includes('timeout') || error.includes('timeout')) failures.push('source_timeout'); if (error.includes('dns') || error.includes('enotfound') || error.includes('eai_again')) failures.push('network_dns_resolution_failure'); if (status.includes('429') || error.includes('429') || obj.http_status === 429) failures.push('rate_limited_http_429'); if (obj.http_status >= 500 || status.includes('5xx') || error.includes('5xx')) failures.push('provider_http_5xx'); if (obj.http_status >= 400 && obj.http_status < 500 && obj.http_status !== 429) failures.push('provider_http_4xx'); if (status.includes('schema') || error.includes('schema')) failures.push('schema_validation_failure'); if (status.includes('stale') || error.includes('stale')) failures.push('stale_source_artifact'); if (status.includes('empty') || error.includes('empty') || obj.empty === true) failures.push('empty_source_result'); if (status.includes('partial')) warnings.push('partial_source_artifact'); if (liveBlocked) warnings.push('live_fetch_disabled_by_policy'); } if (rowEvidence === 0 && source.evidence.some((item) => item.kind === 'result')) { failures.push('empty_source_result'); } const uniqueFailures = Array.from(new Set(failures)); const uniqueWarnings = Array.from(new Set(warnings)); const severity = uniqueFailures.length === 0 ? 'none' : (fallbackAvailable ? 'degraded_with_fallback' : 'blocking'); const action = uniqueFailures.length === 0 ? 'continue_dry_run_contract' : (fallbackAvailable ? 'use_cache_fallback_and_recheck' : 'hold_downstream_consumers'); return { source_id: source.source_id, severity, action, row_evidence: rowEvidence, fallback_available: fallbackAvailable, failures: uniqueFailures, warnings: uniqueWarnings, retry_policy: policy.retry_policy || {}, evidence_count: source.evidence.length }; } function deriveOverall(classifications, governanceIssues) { const blocking = classifications.filter((c) => c.severity === 'blocking'); const degraded = classifications.filter((c) => c.severity === 'degraded_with_fallback'); if (governanceIssues.length > 0) return { status: 'blocked', quality: 'governance_issue' }; if (blocking.length > 0) return { status: 'blocked', quality: 'blocking_source_failures' }; if (degraded.length > 0) return { status: 'degraded', quality: 'fallback_available_recheck_required' }; if (classifications.length === 0) return { status: 'contract_ready', quality: 'no_live_results_to_classify' }; return { status: 'hardening_ready', quality: 'no_blocking_source_failures_detected' }; } function governanceIssues(policy, args) { const issues = []; if (policy.production_automation !== 'OFF') issues.push('production_automation_not_off'); if (policy.forecast_gate !== 'OFF') issues.push('forecast_gate_not_off'); if (policy.promotion_allowed !== false) issues.push('promotion_allowed_not_false'); if (policy.stable_engine_protected !== true) issues.push('stable_engine_not_protected'); if (policy.canonical_workbook_protected !== true) issues.push('canonical_workbook_not_protected'); if (args.allowLive && args.mode !== 'sandbox_live') issues.push('allow_live_requires_sandbox_live_mode'); if (args.allowLive && policy.live_fetch_allowed_by_default !== false) issues.push('live_fetch_default_not_false'); return issues; } function main() { const args = parseArgs(process.argv); const policy = readJsonMaybe(args.policy) || {}; const adapter = readJsonMaybe(args.adapter); const validation = readJsonMaybe(args.validation); const probe = readJsonMaybe(args.probe); const cache = readJsonMaybe(args.cache); const sources = collectSources(adapter, validation, probe, cache); const issues = governanceIssues(policy, args); const classifications = sources.map((source) => classifyEvidence(source, policy)); const overall = deriveOverall(classifications, issues); const status = { schema_version: 'f1_session_live_source_failure_taxonomy_v30q_r2_2026-06-16', generated_utc: nowIso(), mode: args.mode, allow_live: args.allowLive, live_fetch_performed: false, production_automation: 'OFF', forecast_gate: 'OFF', promotion_allowed: false, stable_engine_modified: false, canonical_workbook_overwrite: false, taxonomy_status: overall.status, readiness_quality: overall.quality, governance_issues: issues, source_count: classifications.length, blocking_source_count: classifications.filter((c) => c.severity === 'blocking').length, degraded_source_count: classifications.filter((c) => c.severity === 'degraded_with_fallback').length, classifications, consumer_gates: { workbook_write_enabled: false, canonical_workbook_write_enabled: false, forecast_bundle_write_enabled: false, race_predictions_refresh_enabled: false, fantasy_refresh_enabled: false, notification_send_enabled: false, production_automation_enabled: false }, next_step: 'v30R_forecast_bundle_ledger_snapshot_writer_after_hardening_review' }; writeJson(args.out, status); if (args.log) { writeJson(args.log, { ok: true, generated_utc: status.generated_utc, out: args.out, taxonomy_status: status.taxonomy_status }); } console.log(JSON.stringify({ ok: true, out: args.out, taxonomy_status: status.taxonomy_status, readiness_quality: status.readiness_quality, blocking_source_count: status.blocking_source_count, degraded_source_count: status.degraded_source_count }, null, 2)); } main();
