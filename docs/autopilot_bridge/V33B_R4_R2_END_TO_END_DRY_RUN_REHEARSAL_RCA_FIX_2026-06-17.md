# v33B-R4-R2 RCA Fix

R4 failed in command transport before PR creation because compact/minified email JSON allowed a newline inside the PR title. This R2 keeps the RCA-backed CLI and fixture repair but returns to the validated 1B line-formatted payload pattern.

No production automation, live fetch, workbook write, ledger write, Race/Fantasy refresh, notification, or model promotion is enabled.
